/**
 * @file Kinematics.js
 * @description Handles the math of mapping real-world coordinates to machine space.
 */
export class Kinematics {
  /** mm -> machine units */
  static mmToMachine(mm, axisConfig) {
    // apply offsets, scaling, and clamping
  }

  /** machine units -> mm */
  static machineToMm(units, axisConfig) {
    // reverse the mapping
  }
}
